package com.solo.junit;

import java.util.concurrent.BlockingQueue;
import java.util.concurrent.SynchronousQueue;

public class HelloWorld {

  private static final String INPUT1 = "HELLO";
  private static final String INPUT2 = "WORLD";

  public static void main(String[] args) {

    double myNum = 2.0;
    double myNum2 = 5.0;
    int precision = 10000; //keep 4 digits
    System.out.println(

        myNum = Math.floor((myNum / myNum2) * precision + .5) / precision);
    BlockingQueue<String> stringBlockingQueue = new SynchronousQueue<>();
  }

}
